package poersch.minecraft.bettergrassandleaves.interfaces;

import net.minecraft.Icon;

public interface IBetterCorals {
    Icon getIconBetterCoral(int i, float f);

    Icon getIconBetterCrust(int i, float f);
}
